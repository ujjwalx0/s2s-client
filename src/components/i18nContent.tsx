// // components/i18nContent.tsx
// import { useTranslation } from 'next-i18next';

// type I18nContentProps = {
//   contentKey: string;
// };

// const I18nContent = ({ contentKey }: I18nContentProps) => {
//   const { t } = useTranslation(); // Using i18next hook to get translations

//   return <div>{t(contentKey)}</div>; // Fetches and renders the translation
// };

// export default I18nContent;
